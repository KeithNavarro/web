<center>
    <%= form_for(@newPost) do |f| %>
    
      <div class="field">
        <%= f.text_field :content, autofocus: true %>
      </div>
      
      <div class="actions">
        <%= f.submit "Post" %>
      </div>
    <% end %>
</center>

